[[ -z "$project_root" ]]\
    && "project_root is unset!"\
    && exit 1

namespace='github.com/spreadshirt'
project_name="go-feature"
full_path="$project_root/.go/src/$namespace/$project_name"

# vim: set syntax=sh:
